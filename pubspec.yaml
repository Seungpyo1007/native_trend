name: native_trend
description: >-
  Finds Flutter plugins that render iOS 26 Liquid Glass with real native
  views and writes an AI context file to apply them.
version: 0.1.0
repository: https://github.com/Seungpyo1007/native_trend
issue_tracker: https://github.com/Seungpyo1007/native_trend/issues
topics:
  - liquid-glass
  - ios
  - cupertino
  - cli
  - ai

environment:
  sdk: ^3.10.0

executables:
  native_trend:

dependencies:
  args: ^2.7.0
  http: ^1.6.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.32.0
